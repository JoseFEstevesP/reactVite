import { statusOptions } from '@/globalOptions';

export const accountStatusOptions = [
	{ value: 'true', label: 'Sí' },
	{ value: 'false', label: 'No' },
];

export const statusUserOptions = statusOptions;
